<tr>
  <td><%= product.id %></td>
  <td><%= product.name %></td>
  <td><%= product.description %></td>
  <td><%= product.price %></td>
  <td><%= product.category.name %></td>
  <td><%= product.category.name %></td>
  <td><%= available_toggle(product) %></td>
  <td><%=link_to(semantic_icon('play'), product_path(product)) %></td>
  <td><%=  link_to(semantic_icon('edit'), edit_product_path(product)) %></td>
  <td><%=  link_to(semantic_icon('remove'),product, method: :delete) %></td>
</tr>